.env
node_modules/
# dist/ is intentionally tracked so Hostinger can serve the compiled build
# dist/

# Stripe batch scripts (contain secret keys — never commit these)
create-stripe-links.mjs
inject-stripe-links.mjs
stripe-links.json
index.html.bak
